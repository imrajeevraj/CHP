#include <bits/stdc++.h>
using namespace std;

int main()
{
	int t;
	cin >> t; 
	while (t--)
	{
		int n;
		cin >> n; 
		long long sum = 0;
        for (int i = 0; i < n ; i++){
            int m;
            cin >> m ;
            if()
        }
		while (num >= 0) 
		{
			ans.push_back(num);
			num--;
		}
		num = pow(2, msb); 
		while (num <= n) 
		{
			ans.push_back(num);
			num++;
		}

		for (auto it : ans) 
			cout << it << " ";
		cout << endl;
	}
}


